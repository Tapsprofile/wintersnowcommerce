---
- name: Provision fashion-ready WooCommerce store
  hosts: local
  become: true
  vars:
    base_packages:
      - curl
      - git
      - unzip
      - ca-certificates
    web_packages:
      - nginx
    db_packages:
      - mariadb-server
      - mariadb-client
    php_packages:
      - "php{{ php_version }}-fpm"
      - "php{{ php_version }}-cli"
      - "php{{ php_version }}-mysql"
      - "php{{ php_version }}-xml"
      - "php{{ php_version }}-curl"
      - "php{{ php_version }}-gd"
      - "php{{ php_version }}-mbstring"
      - "php{{ php_version }}-zip"
      - "php{{ php_version }}-intl"
      - "php{{ php_version }}-bcmath"
      - "php{{ php_version }}-soap"
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600

    - name: Install base packages
      ansible.builtin.apt:
        name: "{{ base_packages + web_packages + db_packages + php_packages }}"
        state: present

    - name: Ensure services are running
      ansible.builtin.service:
        name: "{{ item }}"
        state: started
        enabled: true
      loop:
        - nginx
        - mariadb
        - "php{{ php_version }}-fpm"

    - name: Ensure site root exists
      ansible.builtin.file:
        path: "{{ site_root }}"
        state: directory
        owner: www-data
        group: www-data
        mode: "0755"

    - name: Install WP-CLI
      ansible.builtin.get_url:
        url: "https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar"
        dest: /usr/local/bin/wp
        mode: "0755"

    - name: Download WordPress core
      ansible.builtin.command: wp core download --path={{ site_root }}
      args:
        creates: "{{ site_root }}/wp-settings.php"
      become_user: www-data

    - name: Create database and user
      ansible.builtin.shell: |
        mysql <<SQL
        CREATE DATABASE IF NOT EXISTS `{{ db_name }}` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
        CREATE USER IF NOT EXISTS '{{ db_user }}'@'localhost' IDENTIFIED BY '{{ db_password }}';
        GRANT ALL PRIVILEGES ON `{{ db_name }}`.* TO '{{ db_user }}'@'localhost';
        FLUSH PRIVILEGES;
        SQL
      args:
        executable: /bin/bash

    - name: Create wp-config.php
      ansible.builtin.command: >
        wp config create
        --path={{ site_root }}
        --dbname={{ db_name }}
        --dbuser={{ db_user }}
        --dbpass={{ db_password }}
        --dbhost=localhost
        --skip-check
      args:
        creates: "{{ site_root }}/wp-config.php"
      become_user: www-data

    - name: Set WordPress config defaults
      ansible.builtin.command: >
        wp config set {{ item.key }} {{ item.value }}
        --type=constant
        --path={{ site_root }}
      loop:
        - { key: "FS_METHOD", value: "direct" }
        - { key: "WP_MEMORY_LIMIT", value: "256M" }
      become_user: www-data

    - name: Check if WordPress is installed
      ansible.builtin.command: wp core is-installed --path={{ site_root }}
      register: wp_is_installed
      ignore_errors: true
      become_user: www-data

    - name: Install WordPress
      ansible.builtin.command: >
        wp core install
        --path={{ site_root }}
        --url={{ wp_site_url }}
        --title={{ wp_site_title }}
        --admin_user={{ wp_admin_user }}
        --admin_password={{ wp_admin_password }}
        --admin_email={{ wp_admin_email }}
      when: wp_is_installed.rc != 0
      become_user: www-data

    - name: Install and activate theme
      ansible.builtin.command: >
        wp theme install {{ wp_theme_slug }}
        --activate
        --force
        --path={{ site_root }}
      become_user: www-data

    - name: Install and activate public plugins
      ansible.builtin.command: >
        wp plugin install {{ item }}
        --activate
        --force
        --path={{ site_root }}
      loop: "{{ wp_plugins }}"
      when: wp_plugins | length > 0
      become_user: www-data

    - name: Flag premium plugins for manual install
      ansible.builtin.debug:
        msg: "Install premium plugin manually: {{ item }}"
      loop: "{{ wp_premium_plugins }}"
      when: wp_premium_plugins | length > 0

    - name: Write nginx site config
      ansible.builtin.template:
        src: nginx-site.conf.j2
        dest: /etc/nginx/sites-available/fashion-store.conf
        mode: "0644"
      notify: Reload nginx

    - name: Enable nginx site
      ansible.builtin.file:
        src: /etc/nginx/sites-available/fashion-store.conf
        dest: /etc/nginx/sites-enabled/fashion-store.conf
        state: link
      notify: Reload nginx

    - name: Disable default nginx site
      ansible.builtin.file:
        path: /etc/nginx/sites-enabled/default
        state: absent
      notify: Reload nginx

    - name: Ensure wp-content is writable
      ansible.builtin.file:
        path: "{{ site_root }}/wp-content"
        state: directory
        owner: www-data
        group: www-data
        mode: "0755"

  handlers:
    - name: Reload nginx
      ansible.builtin.service:
        name: nginx
        state: reloaded
